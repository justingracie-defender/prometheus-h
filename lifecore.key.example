# LifeCore local signing key placeholder
#
# Do not put a real private key in this file.
# The application creates lifecore.key automatically on first run when no key exists.
# Keep generated *.key files local, ignored by Git, and rotate them if they were ever committed or shared.
